#include<iostream>

using namespace std;

void insertion(int a[],int len,int sindx){
	int key = a[sindex];
	for(int i=0; i<len;i++){
		if(a[i+1] <= key)
	}
}

int main(){
	int a[] = {10,6,5,8,4};
	int len = sizeof(a)/sizeof(a[0]);
	insertion(a,len,0);
	return 0;
}
